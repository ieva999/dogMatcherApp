$(window.onload = function() {
    if (window.jQuery) {
        // jQuery is loaded
        document.write("IT DOES  WORK");  
      } else {
        // jQuery is not loaded
        document.write("IT DOES NOT WORK");
    }
  }
)
